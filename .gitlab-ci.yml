default:
  image: harbor.shopeemobile.com/undefined/nodejs:rap-server-1

pages:
  stage: deploy
  script:
    - yarn --ignore-engines
    - yarn site
    - mv _site public
  artifacts:
    paths:
      - public
  only:
    - master
